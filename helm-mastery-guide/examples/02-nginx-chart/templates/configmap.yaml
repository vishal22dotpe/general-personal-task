{{- if .Values.customConfig.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "my-nginx.fullname" . }}-config
  labels:
    {{- include "my-nginx.labels" . | nindent 4 }}
data:
  default.conf: |
    {{- .Values.customConfig.nginxConf | nindent 4 }}
{{- end }}
